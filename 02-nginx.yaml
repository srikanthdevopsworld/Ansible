- name: nginx installation
  hosts: web
  become: yes
  Tasks:
  - name: ngix installation
    ansible.builtin.package:
        name: nginx
        state: present
  - name: start the nginx
    ansible.builtin.service:
        name: nginx
        state: started
        
