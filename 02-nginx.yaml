- name: nginx installation
  hosts: web
  become: yes
  Tasks:
  - name: nginx install
    ansible.builtin.package:
        name: nginx
        state: present
  - name: start the nginx
    ansible.builtin.service:
        name: nginx
        state: started
        
