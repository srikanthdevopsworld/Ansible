- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Install packages
    ansible.builtin.dnf:
      name: "{{ item }}"
      stated: installed
    loop:
    - gcc
    - zip
    - mysql
