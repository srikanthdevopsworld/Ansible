# - name: install packages
#   hosts: local
#   connection: local
#   become: yes
#   tasks:
#   - name: Install packages
#     ansible.builtin.dnf:
#       name: "{{ item }}"
#       stated: installed
#     loop:
#     - gcc
#     - zip
#     - mysql

- name: install packages
  hosts: web
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: installed
    loop:
    - gcc
    - mysql
    - git
    - zip